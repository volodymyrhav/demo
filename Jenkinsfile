pipeline {
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
        stage('test') {
            steps {
                sh 'whoami'
            }         
        }
    }
}
